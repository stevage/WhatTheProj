{
  "name": "what-the-proj",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "npm run build  && rm dist/js/* && rm dist/css/* && cp -pr dist/* ../what-the-proj-gh-pages && (cd ../what-the-proj-gh-pages; git add js/*; git add css/*; git commit -am '.' && git push)",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "guess-projection": "^0.3.0",
    "vue": "^2.5.17"
  },
  "devDependencies": {
    "@vue/cli-plugin-eslint": "^3.1.2",
    "@vue/cli-service": "^3.1.2",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.8.0",
    "eslint-plugin-vue": "^5.0.0-0",
    "vue-template-compiler": "^2.5.17"
  }
}
